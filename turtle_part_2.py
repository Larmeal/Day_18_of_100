#ใช้ เต่า วเคบื่อนที่เป็น มุม 

from turtle import Turtle, Screen, right

timmy = Turtle()
timmy.shape("turtle")

def run():
    timmy.forward(100)
    timmy.right(angle)

for i in range(0, 11):
    angle = 360 / (i + 3)
    for n in range(0, i + 3):
        run()
        








screen = Screen()
screen.exitonclick()